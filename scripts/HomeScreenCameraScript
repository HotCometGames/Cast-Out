using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HomeScreenCameraScript : MonoBehaviour
{
    public float rotationSpeed = 2;
    void Update()
    {
        transform.Rotate(Vector3.up, rotationSpeed * Time.deltaTime);
    }
}
